# Regenerated by run_all.sh; not tracked.
data/derived/*
!data/derived/.gitkeep
logs/*
!logs/.gitkeep
output/tables/*
!output/tables/.gitkeep

# The figures and saved estimates ARE tracked, so anyone can see what the
# package produces without running it first.

Rplots.pdf
.Rhistory
.RData
.DS_Store
